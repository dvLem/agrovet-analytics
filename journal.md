## 2026-09-04
- Настроил Postgres + DBeaver через Docker Desktop (Docker уже был, спасибо докер-компоуз стенду с n8n и Metabase)
- Столкнулся с ошибкой "connection refused" — оказалось, Docker Desktop не был запущен
- Настроил .wslconfig, ограничил WSL2 до 4GB — сработало
- Первый успешный запрос: SELECT version() — PostgreSQL 16.15
- Настроил сертификат Минцифры для доступа к гос.сайтам

## 2026-09-08
- Закрепление SQL: практика по SELECT/WHERE/LIKE, регуляркам (~, ~*), 
  GROUP BY + HAVING, подзапросам (WHERE ... IN)
- Создал тестовую базу на своей ветеринарной тематике: 3 таблицы 
  (owners, patients, visits) с тестовыми данными
- Столкнулся с ошибкой "relation does not exist" — таблицы не создались 
  с первого раза из-за незавершённого предыдущего запуска, разобрался 
  через DROP TABLE + пересоздание
- Прошёл 17 практических задач на закрепление (SELECT/WHERE/LIKE, 
  GROUP BY/HAVING, подзапросы)
- Разобрался, как находить и переоткрывать сохранённые SQL-скрипты 
  в DBeaver (папка Scripts в левой панели)
