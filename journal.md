## 2026-09-04
- Настроил Postgres + DBeaver через Docker Desktop (Docker уже был, спасибо докер-компоуз стенду с n8n и Metabase)
- Столкнулся с ошибкой "connection refused" — оказалось, Docker Desktop не был запущен
- Настроил .wslconfig, ограничил WSL2 до 4GB — сработало
- Первый успешный запрос: SELECT version() — PostgreSQL 16.15
- Настроил сертификат Минцифры для доступа к гос.сайтам

## 2026-09-09

- Практика по SELECT/WHERE/LIKE и регуляркам (~)
- Прошёл задачи 1-4: фильтрация по городу, NOT IN vs !=, 
  LIKE 'И%' vs регулярка ~ 'И'
- Разобрался в важном отличии: LIKE 'И%' ищет с начала строки, 
  а ~ 'И' без ^ ищет вхождение где угодно в строке
- Осталось из блока 1: задачи 5-8 (диагнозы, регулярка на телефон, NULL)

## 2026-09-09 (продолжение)

- Завершил блок 1: задачи 1-8 (SELECT/WHERE/LIKE, регулярки, IS NULL/NOT NULL)
- Нашёл и исправил две содержательные ошибки:
  - LIKE 'Плановый%' vs '%Плановый%' — разница между "начинается с" и "содержит"
  - Регулярка [+7916] — это класс символов (любой ОДИН символ из списка), 
    а не последовательность; правильно: ^\+7916
- Разобрался с алиасами (AS) — переименование колонки должно отражать 
  её смысл, а не быть случайным

## 2026-09-10

### SQL — Блок 2 (GROUP BY + HAVING), задачи 9-13
- Задача 9: количество пациентов на владельца (owner_id, count)
- Задача 10: поиск тёзок среди пациентов (GROUP BY name + HAVING count > 1)
  — разобрался, когда в SELECT нужен count(*), а когда нет (зависит от 
  смысла вопроса: "найти" vs "посчитать")
- Задача 11: подсчёт визитов по пациенту, +усложнил сам — добавил JOIN 
  с patients, чтобы вывести кличку рядом с patient_id
  — разобрался, почему при добавлении обычного поля в SELECT его 
  обязательно нужно дублировать в GROUP BY
- Задача 12: количество пациентов по видам, сортировка по убыванию (кот 3, 
  собака 2, хомяк 1)
- Задача 13: города с несколькими владельцами (Москва, 3 владельца)
  — попробовал два способа вывести имена владельцев вместе с городом: 
  подзапрос с IN, и оконная функция count(*) OVER (PARTITION BY city)
  — разобрался в логике оконных функций: не схлопывают строки, в отличие 
  от GROUP BY, а добавляют вычисленное значение к каждой строке; понял, 
  почему WHERE не может напрямую фильтровать результат оконной функции 
  и зачем нужна обёртка в подзапрос
- Блок 2 полностью закрыт, весь блок 3 (подзапросы) фактически освоен 
  попутно через практику с окнами

### Техническая часть — диагностика ноутбука
- Разобрался с периодическими обрывами Wi-Fi (сеть полностью пропадала 
  ночью): проверил через ipconfig /all, нашёл рабочий способ восстановления 
  без полной перезагрузки — ipconfig /release + /renew, либо перезапуск 
  адаптера через netsh
- Обновил Wi-Fi драйвер Intel Wireless-AC 9462 (версия 24.70.0) — скачал 
  напрямую с сайта Intel, т.к. Dell больше не обновляет для России
- Проверил реальную скорость интернета через speedtest.ru (54/87 Мбит/с — 
  канал в полном порядке, низкие цифры в Диспетчере задач были просто 
  моментальной активностью, а не потолком скорости)
- Обнаружил аппаратную проблему: диагностика Dell SupportAssist показала 
  ошибку вентилятора (Error Code: 2000-0712) — нужно обращаться в 
  сервисный центр (ориентировочно 1500-5000 ₽ в зависимости от того, 
  чистка это или замена)
- Перевёл фоновые Dell-службы (Client Management, Connected Service 
  Delivery, Digital Delivery, TechHub) на "Вручную" — освободил память
- Взял за правило останавливать n8n и metabase, когда работаю только 
  с SQL/Postgres — docker compose stop n8n metabase
  
